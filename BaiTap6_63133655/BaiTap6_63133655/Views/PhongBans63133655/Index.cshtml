@model IEnumerable<BaiTap6_63133655.Models.PhongBan>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    var grid = new WebGrid(Model, canPage: true, defaultSort: "TenPB", rowsPerPage: 5);
}

<h3>DANH SÁCH PHÒNG BAN</h3>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<!--
<table class="table">
    <tr>
        <th>
            Mã PB
        </th>
        <th>
            Tên PB
        </th>
        <th>Chức năng</th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MaPB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenPB)
            </td>
            <td>
                @Html.ActionLink("Sửa", "Edit", new { id = item.MaPB }) |
                @Html.ActionLink("Xem chi tiết", "Details", new { id = item.MaPB }) |
                @Html.ActionLink("Xóa", "Delete", new { id = item.MaPB })
            </td>
        </tr>
    }

</table>
-->
<html>
<head>
    <style type="text/css">
        .grid-footer {
            color: #000;
            font: 17px Calibri;
            text-align: center;
            font-weight: bold;
        }

            .grid-footer a {
                background-color: #ffffff;
                color: blue;
                box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
                padding: 1px 10px 2px 10px;
            }

                .grid-footer a:active, a:hover {
                    background-color: #ffffff;
                    color: #FFAD33;
                    box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
                }

        .alternatingRowStyle {
            background-color: aliceblue;
        }

        table .table1 {
            text-align: left;
            margin-left: 0px;
            margin-right: 0px;
            width: 800px;
        }

        .tr, .td {
            text-align: left;
        }
    </style>
</head>
<body>
    <div id="gridContent">
        @ViewBag.TB
        @grid.GetHtml(
            tableStyle: "table table-bordered table-hover",
            footerStyle: "grid-footer",
            fillEmptyRows: true,
            mode: WebGridPagerModes.Numeric,
            alternatingRowStyle: "alternatingRowStyle",
              columns: new[]  // colums in grid
              {
                    grid.Column("STT",format: item => item.WebGrid.Rows.IndexOf(item) + 1 + Math.Round(Convert.ToDouble(grid.TotalRowCount / grid.PageCount) / grid.RowsPerPage) * grid.RowsPerPage * grid.PageIndex),
                    grid.Column("MaPB","Mã phòng ban"), //the model fields to display
                    grid.Column("TenPB","Tên phòng ban"),
                    grid.Column(format: (item) => Html.ActionLink("Cập nhật", "Edit", "PhongBans63133655",new { id = item.MaPB },"PhongBans63133655Controller")),
                    grid.Column(format: (item) => Html.ActionLink("Xem chi tiết", "Details","PhongBans63133655", new { id = item.MaPB },"PhongBans63133655Controller")),
                    grid.Column(format: (item) => Html.ActionLink("Xóa", "Delete","PhongBans63133655", new { id = item.MaPB },"PhongBans63133655Controller")),
                    grid.Column("Chức năng", format: @<text>
    <a href="@Url.Action("Edit",
                            new { id = item.MaPB})" class="edit-btn"><abbr title="Cập nhật dữ liệu"><img src="/images/edit.png" width="20" height="20" /></abbr></a>
    <a href="@Url.Action("Details",
                            new { id = item.MaPB})" class="edit-btn"><abbr title="Xem chi tiết"><img src="/images/details.png" width="20" height="20" /></abbr></a>
    <a href="@Url.Action("Delete",
                            new { id = item.MaPB})" class="edit-btn"><abbr title="Xóa dữ liệu"><img src="/images/delete.png" width="20" height="20" /></abbr></a>
</text>),
}
)
    </div>
</body>
</html>